export const WHITELIST = [
  'swop.fi',
  'vires.finance',
  'app.power.tech',
  'wx.network',
  'decentralchain.io',
];

// TODO: Replace with actual DCC test domains once available
export const CUSTOMLIST = ['docs.decentralchain.io', 'decentralchain.io'];

export const DEFAULT_PASSWORD = 'default-password';

export const DEFAULT_ANIMATION_DELAY = 800;
export const DEFAULT_PAGE_LOAD_DELAY = 800;

export const DEFAULT_MINER_SEED = 'dcc private node seed with dcc tokens';
export const ISSUER_SEED = 'issuer account that will receive tokens from generator';
export const USER_1_SEED = 'user number one that used in test scenarios';
export const USER_2_SEED = 'user number two that used in test scenarios';
