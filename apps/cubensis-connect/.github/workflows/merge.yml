name: Merge
on:
  push:
    branches:
      - master
jobs:
  report:
    runs-on: ubuntu-latest
    steps:
      - uses: styfle/cancel-workflow-action@0.11.0
      # TODO(DCC-59): Add DCC merge notification hook
      # Previously used Keeper-Wallet/jira-action — needs DCC equivalent
