name: Release
on:
  release:
    types: [published]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  report:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Release workflow triggered"
      # TODO(DCC-59): Add DCC release notification hook
      # Previously used Keeper-Wallet/jira-action — needs DCC equivalent
